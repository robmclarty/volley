allowBuilds:
  esbuild: true

# fascicle and checkride are authored in-house and trusted; exempt them from
# pnpm's minimumReleaseAge supply-chain quarantine (48h here) so fresh releases
# install immediately. The bare names cover every future version.
minimumReleaseAgeExclude:
  - fascicle
  - checkride
